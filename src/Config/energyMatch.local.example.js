/**
 * Copy to `energyMatch.local.js` (gitignored) and set your deployed Django URL.
 * Required for Energy Match on a release APK installed on a physical phone.
 *
 *   cp src/Config/energyMatch.local.example.js src/Config/energyMatch.local.js
 */
export const ENERGY_MATCH_PROD_BASE_URL = 'https://your-energy-match-api.onrender.com';
